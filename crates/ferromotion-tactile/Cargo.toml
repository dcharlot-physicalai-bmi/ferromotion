[package]
name = "ferromotion-tactile"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
homepage.workspace = true
repository.workspace = true
keywords.workspace = true
categories.workspace = true
description = "Differentiable optical-tactile (GelSight/DIGIT) sensor simulation — gel deformation → photometric image. WASM-clean."

[dependencies]
nalgebra.workspace = true

# Test-only. The elastic module's influence function is DATA, and the validation measures it with a
# real 3D solve rather than trusting a closed form. Runtime dependencies stay nalgebra-only, so the
# crate is still WASM-clean.
[dev-dependencies]
ferromotion-fem = { path = "../ferromotion-fem", version = "0.67.0" }

[lints]
workspace = true
